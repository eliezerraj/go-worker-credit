apiVersion: external-secrets.io/v1beta1 
kind: ExternalSecret 
metadata: 
  name: &app-name es-go-worker-balance
  namespace: test-a
  labels:
    app: *app-name
spec: 
  refreshInterval: 1h 
  secretStoreRef: 
    name: ss-sa-go-worker-balance
    kind: SecretStore 
  target: 
    name: es-rds-arch-secret-go-worker-balance
    creationPolicy: Owner 
  dataFrom: 
  - extract: 
      key: rds-arch-secret